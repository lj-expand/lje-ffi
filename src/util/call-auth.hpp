#pragma once
#include "../globals.hpp"

// Bygone artifact from an earlier LJE version where, there was a chance
// something *could* grab any of our functions. Not anymore.
#define FFI_AUTH_CALL(lua, state)
